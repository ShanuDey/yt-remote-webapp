import RemoteSetup from '../../components/RemoteSetup';
import Layout from '../../components/Layout';

const remote_setup = () => {
  return (
    <Layout>
      <RemoteSetup />
    </Layout>
  );
};

export default remote_setup;
